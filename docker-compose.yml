version: '3'
services:
  app:
    image: ameeuwsen/leaks:latest
    ports:
      - "8666:80"
    networks:
      - leaks_network
    volumes:
      - dumps:/var/www/html/dumps
volumes:
  dumps:
    external: true
networks:
  leaks_network:
